server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/warehouse
    username: user
    password: user123
  jpa:
    show-sql: false
    jdbc:
      time_zone: UTC
    hibernate:
      ddl-auto: update
